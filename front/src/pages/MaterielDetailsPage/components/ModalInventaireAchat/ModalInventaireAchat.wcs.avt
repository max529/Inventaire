:host {
    .title {
		font-size: var(--font-size-md);
		margin-bottom: 16px;
	}
	.form {
		display: flex;
		flex-direction: column;
		gap: 12px;
	}
    .footer {
		display: flex;
		justify-content: flex-end;
		margin-top: 2rem;
        gap: 0.5rem;
	}
}